<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDark</item>
        <item name="colorPrimaryDark">@color/colorDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle"> @color/colorDark</item>
    <item name="android:actionButtonStyle">@color/colorDark</item>

        <item name="colorButtonNormal">@color/colorDark</item>
        <item name="android:colorButtonNormal">@color/colorDark</item>
    </style>

</resources>
